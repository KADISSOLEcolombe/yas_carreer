'use client';

/** Nav items kept for any legacy imports; layout uses DashboardShell directly. */
export const ADMIN_NAV_ITEMS = [] as const;

export default function AdminSidebar() {
  return null;
}
